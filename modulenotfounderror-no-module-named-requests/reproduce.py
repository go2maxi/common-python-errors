import requests

# Network calls might fail depending on SSL certificates or environment,
# but the import statement itself will fail if the package is not installed.

# response = requests.get("https://example.com")
# print(response.status_code)